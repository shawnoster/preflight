# Local configuration files (use .template versions as starting point)
config/accounts.sh
config/owl.sh
lib/1password.sh

# Runtime state
state/

# Environment-specific overrides
.env
.env.local

# Claude Code local settings
.claude/settings.local.json
