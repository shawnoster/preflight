#!/usr/bin/env bash
# Example .bashrc addition - add this near the end of your .bashrc
#
# Load developer environment
# Set DEV_VERBOSE=1 to see confirmation message
[[ -f "$HOME/.dev/init.sh" ]] && source "$HOME/.dev/init.sh"
